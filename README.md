# GenAI + Prometheus Chatbot 🤖📊

A chatbot that uses an LLM (via Ollama) to generate PromQL queries from natural language, and fetches data from Prometheus.

## 🚀 How to Run

```bash
pip install -r requirements.txt
streamlit run app.py
